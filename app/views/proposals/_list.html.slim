- unless proposals.empty?
  ul.proposals.list
    - proposals.each do |proposal|
      li.proposals.item
        == render :partial => "proposals/single", :locals => { :proposal => proposal, :assembly => assembly }
- else
  p.proposals.none = t('proposals.none')
